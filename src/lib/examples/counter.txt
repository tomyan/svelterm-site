<script>
    let count = $state(0)
</script>

<div class="counter">
    <span class="title">Counter</span>
    <span class="value">{count}</span>
    <button onclick={() => count++}>Increment</button>
    <button onclick={() => count--}>Decrement</button>
</div>

<style>
    :root { color-scheme: light dark; }
    :root[data-theme="light"] { color-scheme: light; }
    :root[data-theme="dark"]  { color-scheme: dark; }

    :root {
        --text:        light-dark(#1a1a2e, #e8e8e8);
        --accent:      light-dark(#0891b2, #48cae4);
        --accent-warm: light-dark(#d97706, #fbbf24);
        --btn-border:  light-dark(#999, #555);
    }

    .counter {
        display: flex;
        flex-direction: column;
        color: var(--text);

        @media (display-mode: browser) {
            margin: 0 1ch;
            gap: 1em;
            padding: 1em 2ch;
            font-family: ui-monospace, 'SF Mono', Menlo, monospace;
            border: 2px solid var(--accent);
            border-radius: 8px;
        }

        @media (display-mode: terminal) {
            margin: 0 1cell;
            gap: 1cell;
            padding: 1cell 2cell 0 2cell;
            border: rounded;
            border-color: var(--accent);
        }
    }

    .title { color: var(--accent); font-weight: bold; }
    .value { color: var(--accent-warm); font-weight: bold; }

    button {
        color: var(--text);
        border-color: var(--btn-border);
        background: transparent;
        font-family: inherit;

        @media (display-mode: browser) {
            border: 1px solid var(--btn-border);
            padding: 0 2ch;
            cursor: pointer;
            font-size: inherit;
        }

        @media (display-mode: terminal) {
            border: single;
            padding: 0 2cell;
        }
    }

    button:focus {
        border-color: var(--accent-warm);
        color: var(--accent-warm);
        font-weight: bold;
    }

    button:hover {
        border-color: var(--accent);
        color: var(--accent);
    }
</style>
